using UnityEngine;

[RequireComponent(typeof(MeshFilter))]
[RequireComponent(typeof(MeshRenderer))]
public class HexTileStructure : MonoBehaviour
{
    //Currently Nothing to do. Just needed monobehaviour and requireComponents to be sure there are meshfilter and meshrenderer on gameobject.
}
